description: Deploys central and two Kubernetes clusters for a backup and restore demonstration
cloud: aws
clusters: 3
aws_type: t3.2xlarge
aws_ebs: "gp2:80 gp2:100"
k8s_version: 1.16.0
scripts: ["backup"]

# use `-e install_apps=true` to install the optional apps
