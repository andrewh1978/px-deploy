

$cluster env variable must be set individually

aws_ebs -> TODO (see vagrantfile, node vm definition)

post_script TODO

get PXDUSER from container ENV -> maybe set a new env variable


check this:
// TODO if yaml['platform'] == "ocp4" or yaml['platform'] == "eks" or yaml['platform'] == "gke" or yaml['platform'] == "aks" then yaml['nodes'] = 0 end